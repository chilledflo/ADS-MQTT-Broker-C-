# Documentation build configuration
# Minimal CMakeLists.txt for docs directory

# Currently no build steps required for docs
# Documentation is provided as markdown files
message(STATUS "Documentation directory configured")
